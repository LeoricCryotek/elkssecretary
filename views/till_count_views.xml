<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ═══════════════════════════════════════════════════════════
         Till Count — Form
         ═══════════════════════════════════════════════════════════ -->
    <record id="view_till_count_form" model="ir.ui.view">
        <field name="name">elks.till.count.form</field>
        <field name="model">elks.till.count</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_populate_denominations"
                            string="Fill All Denominations"
                            type="object"
                            class="btn-secondary"
                            invisible="state != 'draft'"
                            help="Pre-fill all denomination lines with zero quantity"/>
                    <button name="action_done"
                            string="Mark Done"
                            type="object"
                            class="btn-success"
                            invisible="state != 'draft'"/>
                    <button name="action_reset_draft"
                            string="Re-open"
                            type="object"
                            invisible="state != 'done'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="till_name" placeholder="Till Name"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="session_id"/>
                            <field name="session_date"/>
                            <field name="counted_by"/>
                        </group>
                        <group>
                            <field name="total_bills"/>
                            <field name="total_rolled"/>
                            <field name="total_loose"/>
                            <field name="total"
                                   class="oe_subtotal_footer_separator"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Denominations" name="denominations">
                            <field name="denomination_ids" nolabel="1">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="category"
                                           decoration-bf="1"/>
                                    <field name="denomination"/>
                                    <field name="quantity"/>
                                    <field name="face_value"/>
                                    <field name="subtotal" sum="Total"/>
                                </list>
                            </field>
                        </page>
                        <page string="Notes" name="notes">
                            <field name="note" placeholder="Notes about this till count..."/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- ═══════════════════════════════════════════════════════════
         Till Count — List
         ═══════════════════════════════════════════════════════════ -->
    <record id="view_till_count_list" model="ir.ui.view">
        <field name="name">elks.till.count.list</field>
        <field name="model">elks.till.count</field>
        <field name="arch" type="xml">
            <list>
                <field name="session_date"/>
                <field name="till_name"/>
                <field name="total_bills" sum="Bills"/>
                <field name="total_rolled" sum="Rolled"/>
                <field name="total_loose" sum="Loose"/>
                <field name="total" sum="Grand Total"
                       decoration-bf="1"/>
                <field name="counted_by"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-success="state == 'done'"/>
            </list>
        </field>
    </record>

    <!-- ═══════════════════════════════════════════════════════════
         Till Count — Search
         ═══════════════════════════════════════════════════════════ -->
    <record id="view_till_count_search" model="ir.ui.view">
        <field name="name">elks.till.count.search</field>
        <field name="model">elks.till.count</field>
        <field name="arch" type="xml">
            <search>
                <field name="till_name"/>
                <field name="session_date"/>
                <field name="counted_by"/>
                <filter string="Counting" name="counting"
                        domain="[('state', '=', 'draft')]"/>
                <filter string="Done" name="done"
                        domain="[('state', '=', 'done')]"/>
                <filter string="Group by Session" name="group_session"
                        context="{'group_by': 'session_id'}"/>
            </search>
        </field>
    </record>

    <record id="action_till_counts" model="ir.actions.act_window">
        <field name="name">Till Counts</field>
        <field name="res_model">elks.till.count</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
